spring:
  profiles:
    active: prod2

eureka:
  client:
    service-url:
      defaultZone: http://${zuihou.eureka.hostname.prod1}:${zuihou.eureka.server.port.prod1}/eureka/,http://${zuihou.eureka.hostname.prod2}:${zuihou.eureka.server.port.prod2}/eureka/
