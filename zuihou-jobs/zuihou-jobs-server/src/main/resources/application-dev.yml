zuihou:
  mysql:
    ip: ${MYSQL_IP:127.0.0.1}
    port: 3306
    username: root
    password: root

xxl.job:
  executor:
    ip: ${EXECUTOR_IP:127.0.0.1}
    port: ${EXECUTOR_PORT:8771}

spring:
  profiles:
    active: dev
